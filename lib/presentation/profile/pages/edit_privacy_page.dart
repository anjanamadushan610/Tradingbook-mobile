import 'package:flutter/material.dart';

class EditPrivacyPage extends StatelessWidget {
  const EditPrivacyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Edit Privacy'),
      ),
      body: const Center(
        child: Text('Edit Privacy'),
      ),
    );
  }
}
